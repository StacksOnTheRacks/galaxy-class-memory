---
doc: architecture.decisions
schema_version: 1
updated: 2026-09-12
active_decisions:
  - "ADR-001: Host Chrome extension — party-tab stay, host-bar console, no capture — Status: Accepted (amended 2026-09-12: Room sidebar tab is no longer the sole host control panel; SPA host chrome is HostTheaterButtonBar + Watch Party Settings; prior amendment 2026-08-15: Side Panel retired; page-initiated bridge + host-local Next Up). Context: Hosts need to open/navigate the host media tab and change titles without leaving the watch-party tab, including inside the installed PWA. Capture already lives in the SPA (useHostScreenCapture → getDisplayMedia → RoomMediaEngine → SFU host_screen). Decision: The host Chrome MV3 extension (1) binds the room from the party tab URL /room/:roomId (C1), (2) opens/navigates the host media tab without stealing focus and reports media-tab open/not + play/pause controllability, (3) does not host a Chrome Side Panel — host room-admin UI is the host-bar Watch Party Settings dialog; Load Media / broadcast / transport stay on HostTheaterButtonBar. Catalog title changes use SPA PATCH { catalogEpisodeId } (A1). Page↔extension bridge (riffsync-host-bridge v1) supports JWT A and page-initiated ping/media-tab/playback. Packaging: apps/host-extension; permissions tabs + API host_permissions; no sidePanel; no capture APIs. Consequences: Host UI works in normal Chrome and desktop PWA. Extension is tabs/media helper only. Find/focus return-to-share remains Icebox. Next Up is host-local (localStorage), not guest-visible or server-synced; Next Up UI hidden by host-bar-room-control initiative. No-capture, C1 bind, JWT A, A1, and host-local Next Up storage unchanged."
  - "Product metrics v1 (#437): GA4 client events + CloudWatch RiffSync/Product server EMF; solo watch GA4-only in v1; canonical contract at docs/operations/product-metrics.md."
superseded: []
---
