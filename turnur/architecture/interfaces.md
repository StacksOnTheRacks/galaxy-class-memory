# External interfaces

### HTTP API (API Gateway HTTP API v2)

| Method | Path | Auth | Success response |
| --- | --- | --- | --- |
| GET | `/v1/health` | none | **200** `Content-Type: application/json; charset=utf-8` — body `{ "ok": true }` (stable v1; additive fields only) |

Only GET is registered on the HTTP API for this route.

# Internal boundaries

# Contracts in flight

# Ownership
