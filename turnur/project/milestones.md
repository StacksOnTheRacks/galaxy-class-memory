# Active

-

# Upcoming

- Coarse Now: running control plane (TypeScript / Node 22 / AWS CDK, `GET /v1/health`), then game authentication, then match-state primitives. Memory projection only; no host milestone URL. Board is empty (https://github.com/StacksOnTheRacks/turnur). Shared project already linked (https://github.com/users/StacksOnTheRacks/projects/1).

# Slipped

-
